syntax = "proto3";

import "sync_tooling_msgs/port_id.proto";
import "sync_tooling_msgs/port_state.proto";

// An update reporting the state of a PTP port.
message PortStateUpdate {
  // The ID of the port
  PortId port_id = 1;
  // The state of the port
  PortState port_state = 2;
}