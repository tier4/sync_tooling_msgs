syntax = "proto3";

// A unique clock ID for a sensor. It consists of an arbitrary sensor name and its IP address.
message SensorId {
  // The name of the sensor.
  string name = 1;
  // The IP address of the sensor
  string ip = 2;
}